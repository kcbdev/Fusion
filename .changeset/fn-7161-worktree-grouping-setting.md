---
"@runfusion/fusion": minor
---

summary: Add a project setting to always show worktree names and grouping on the board.
category: feature
dev: New project setting `showWorktreeGrouping` (default false). When true, Column groups WIP tasks by worktree in both legacy and workflow modes.
